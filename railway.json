{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd artifacts/django-backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd artifacts/django-backend && gunicorn islamictube.wsgi:application",
    "restartPolicyType": "ON_FAILURE"
  }
}